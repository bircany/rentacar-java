package com.rentacar.config;

import org.springframework.context.annotation.Configuration;

/**
 * Swagger configuration
 * Note: As this is a Swing application, Swagger is not active.
 * This class can be used when a web interface is added in the future.
 */
@Configuration
public class SwaggerConfig {
    // Swagger configuration will be activated when a web interface is added
} 